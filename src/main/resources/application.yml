spring:
  datasource:
    url: jdbc:postgresql://192.168.12.238:5432/rsqtest
    username: rsqtest
    password: rkskek12
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
        show_sql: true
      generate-ddl: true
    hibernate:
      ddl-auto: update
      generate-ddl: true
      properties:
        temp:
          use_jdbc_metadata_defaults: false
